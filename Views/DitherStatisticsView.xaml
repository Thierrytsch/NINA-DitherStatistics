<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="DitherStatistics.Plugin.DitherStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- NINA StandardCheckBox Style - exact copy from NINA source -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Width" Value="65" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <!-- Track -->
                            <Border
                                x:Name="SwitchTrack"
                                Background="Transparent"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12.5" />
                            <Grid>
                                <TextBlock
                                    x:Name="OnText"
                                    Margin="8,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontWeight="Bold"
                                    Foreground="{DynamicResource ButtonForegroundBrush}"
                                    Text="ON"
                                    Visibility="Collapsed" />
                                <TextBlock
                                    x:Name="OffText"
                                    Margin="0,0,8,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource PrimaryBrush}"
                                    Text="OFF"
                                    Visibility="Visible" />
                            </Grid>
                            <!-- Thumb -->
                            <Ellipse
                                x:Name="SwitchThumb"
                                Width="21"
                                Height="21"
                                Margin="2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Fill="{DynamicResource SecondaryBackgroundBrush}" />
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="OnText" Property="Visibility" Value="Visible" />
                                <Setter TargetName="OffText" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="Transparent" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Left" />
                                <Setter TargetName="OnText" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="OffText" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.4" />
                                <Setter TargetName="SwitchThumb" Property="Opacity" Value="0.4" />
                                <Setter TargetName="OnText" Property="Opacity" Value="0.4" />
                                <Setter TargetName="OffText" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QualityButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock x:Name="contentText"
                                      Text="{TemplateBinding Content}"
                                      Foreground="{TemplateBinding Foreground}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      TextAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonBackgroundSelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonBackgroundSelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete button for statistics profiles - disabled while Default is selected -->
        <Style x:Key="DeleteProfileButton" TargetType="Button" BasedOn="{StaticResource QualityButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedProfileName}" Value="Default">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Minimal dark-theme editable ComboBox (no NINA ComboBox style is reachable from the plugin).
             The PART_EditableTextBox and PART_Popup names are required for editable behavior. -->
        <Style x:Key="ProfileComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                        <Setter Property="Padding" Value="6,3"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="bd" Property="Background"
                                                    Value="{DynamicResource ButtonBackgroundSelectedBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"/>
                            <ToggleButton Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          HorizontalAlignment="Stretch"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{DynamicResource PrimaryBrush}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Margin="4,0,20,0"
                                     VerticalAlignment="Center"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{DynamicResource PrimaryBrush}"
                                     CaretBrush="{DynamicResource PrimaryBrush}"
                                     Focusable="True"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   StaysOpen="False">
                                <Border MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">

                <!-- Pixel Shift Chart (ScottPlot) with Tooltip Overlay -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}" 
                       BorderBrush="{DynamicResource BorderBrush}" 
                       BorderThickness="1" 
                       Padding="10" 
                       Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Pixel Shift" 
                                  FontSize="14" 
                                  FontWeight="SemiBold" 
                                  Foreground="{DynamicResource PrimaryBrush}"
                                  Margin="0,0,0,10"/>

                        <!-- Grid for Chart + Tooltip Overlay -->
                        <Grid Height="300">
                            <!-- ScottPlot WpfPlot Control -->
                            <ContentControl Content="{Binding PixelShiftPlot}"/>

                            <!-- Tooltip Overlay -->
                            <Border BorderBrush="#FF00A0FF" 
                                   BorderThickness="2"
                                   CornerRadius="3"
                                   Padding="8"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   Margin="10"
                                   Opacity="0.95"
                                   Background="{DynamicResource TertiaryBackgroundBrush}"
                                   Visibility="{Binding PixelShiftTooltipVisible, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding PixelShiftTooltipText}"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          FontSize="13"
                                          FontWeight="SemiBold"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Settle Time Chart (ScottPlot) with Tooltip Overlay -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}" 
                       BorderBrush="{DynamicResource BorderBrush}" 
                       BorderThickness="1" 
                       Padding="10" 
                       Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Settle Time History" 
                                  FontSize="14" 
                                  FontWeight="SemiBold" 
                                  Foreground="{DynamicResource PrimaryBrush}"
                                  Margin="0,0,0,10"/>

                        <!-- Grid for Chart + Tooltip Overlay -->
                        <Grid Height="150">
                            <!-- ScottPlot WpfPlot Control -->
                            <ContentControl Content="{Binding SettleTimePlot}"/>

                            <!-- Tooltip Overlay -->
                            <Border BorderBrush="#FF1E90FF" 
                                   BorderThickness="2"
                                   CornerRadius="3"
                                   Padding="8"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   Margin="10"
                                   Opacity="0.95"
                                   Background="{DynamicResource TertiaryBackgroundBrush}"
                                   Visibility="{Binding SettleTimeTooltipVisible, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding SettleTimeTooltipText}"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          FontSize="13"
                                          FontWeight="SemiBold"/>
                            </Border>
                        </Grid>

                        <!-- Legend below the chart -->
                        <StackPanel Orientation="Horizontal" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,8,0,0">
                            <!-- Settle Time -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,15,0">
                                <Ellipse Width="10" Height="10" 
                                        Fill="DodgerBlue" 
                                        VerticalAlignment="Center"
                                        Margin="0,0,4,0"/>
                                <TextBlock Text="Settle Time" 
                                          FontSize="10" 
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Average -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,15,0">
                                <Rectangle Width="14" Height="2" 
                                          Fill="Red" 
                                          VerticalAlignment="Center"
                                          Margin="0,0,4,0">
                                    <Rectangle.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="Red" Offset="0.0"/>
                                            <GradientStop Color="Transparent" Offset="0.3"/>
                                            <GradientStop Color="Red" Offset="0.4"/>
                                            <GradientStop Color="Transparent" Offset="0.6"/>
                                            <GradientStop Color="Red" Offset="0.7"/>
                                            <GradientStop Color="Transparent" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Text="Average" 
                                          FontSize="10" 
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Avg ± StdDev -->
                            <StackPanel Orientation="Horizontal">
                                <Rectangle Width="14" Height="2" 
                                          VerticalAlignment="Center"
                                          Margin="0,0,4,0">
                                    <Rectangle.Fill>
                                        <SolidColorBrush Color="Red" Opacity="0.5"/>
                                    </Rectangle.Fill>
                                    <Rectangle.OpacityMask>
                                        <DrawingBrush TileMode="Tile" Viewport="0,0,3,2" ViewportUnits="Absolute">
                                            <DrawingBrush.Drawing>
                                                <GeometryDrawing Brush="Red">
                                                    <GeometryDrawing.Geometry>
                                                        <RectangleGeometry Rect="0,0,1,2"/>
                                                    </GeometryDrawing.Geometry>
                                                </GeometryDrawing>
                                            </DrawingBrush.Drawing>
                                        </DrawingBrush>
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Text="Avg ± StdDev" 
                                          FontSize="10" 
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- COMPACT Statistics Panel - 2 rows -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}" 
                       BorderBrush="{DynamicResource BorderBrush}" 
                       BorderThickness="1" 
                       Padding="15" 
                       Margin="0,0,0,10">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                          Text="Statistics"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1"
                                          Text="Keep across sessions"
                                          FontSize="10"
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>

                                <CheckBox Grid.Column="2"
                                         IsChecked="{Binding IsStatisticsPersistenceEnabled, Mode=TwoWay}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,5,0"
                                         ToolTip="When enabled, all statistics are saved to disk and restored on the next NINA start, accumulating across sessions"
                                         Style="{DynamicResource ToggleSwitchStyle}"/>
                            </Grid>

                            <!-- Statistics profiles: toggle + editable ComboBox + add/delete -->
                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                          Text="Profiles"
                                          FontSize="10"
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>

                                <CheckBox Grid.Column="1"
                                         IsChecked="{Binding IsMultiProfileEnabled, Mode=TwoWay}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"
                                         ToolTip="Store statistics in separate selectable profiles (e.g. per target or telescope)"
                                         Style="{DynamicResource ToggleSwitchStyle}"/>

                                <ComboBox Grid.Column="2"
                                         Style="{StaticResource ProfileComboBoxStyle}"
                                         IsEditable="True"
                                         ItemsSource="{Binding ProfileNames}"
                                         SelectedItem="{Binding SelectedProfileName, Mode=TwoWay}"
                                         Text="{Binding ProfileNameInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalAlignment="Center"
                                         MinWidth="110"
                                         Height="25"
                                         Visibility="{Binding IsMultiProfileEnabled, Converter={StaticResource BoolToVis}}"
                                         ToolTip="Select a profile, or type a new name and press +"/>

                                <Button Grid.Column="3"
                                       Content="&#x2795;"
                                       Style="{StaticResource QualityButton}"
                                       Command="{Binding CreateProfileCommand}"
                                       Margin="5,0,0,0"
                                       Padding="6,4"
                                       Visibility="{Binding IsMultiProfileEnabled, Converter={StaticResource BoolToVis}}"
                                       ToolTip="Create the profile typed in the box"/>

                                <Button Grid.Column="4"
                                       Content="&#x1F5D1;"
                                       Style="{StaticResource DeleteProfileButton}"
                                       Command="{Binding DeleteProfileCommand}"
                                       Margin="5,0,0,0"
                                       Padding="6,4"
                                       Visibility="{Binding IsMultiProfileEnabled, Converter={StaticResource BoolToVis}}"
                                       ToolTip="Delete the selected profile (Default cannot be deleted)"/>
                            </Grid>
                        </Grid>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Row 1: Dithers (full width) -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                                      Margin="0,0,0,6">
                                <Run Text="Dithers: " Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding TotalDithers, Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text="/" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text="{Binding SuccessfulDithers, Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text="/" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text="{Binding SuccessRate, StringFormat='{}{0:F1}%', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text=" (Total/Success/Rate)" FontSize="11" Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                            </TextBlock>

                            <!-- Row 2, Left: Median -->
                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                      Margin="0,0,10,0">
                                <Run Text="Median: " Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding MedianSettleTime, StringFormat='{}{0:F2}s', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text=", (Min: " FontSize="11" Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding MinSettleTime, StringFormat='{}{0:F2}s', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="11"/>
                                <Run Text=" / Max: " FontSize="11" Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding MaxSettleTime, StringFormat='{}{0:F2}s', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="11"/>
                                <Run Text=")" FontSize="11" Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                            </TextBlock>

                            <!-- Row 2, Right: Drift -->
                            <TextBlock Grid.Row="1" Grid.Column="1">
                                <Run Text="Drift: X: " Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding TotalDriftX, StringFormat='{}{0:F2}px', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                <Run Text=" / Y: " Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                <Run Text="{Binding TotalDriftY, StringFormat='{}{0:F2}px', Mode=OneWay}" 
                                     Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Quality Metrics Panel - Shown when enough data -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}" 
                       BorderBrush="{DynamicResource BorderBrush}" 
                       BorderThickness="1" 
                       Padding="15" 
                       Margin="0,0,0,10"
                       Visibility="{Binding ShowQualityAssessment, Converter={StaticResource BoolToVis}, Mode=OneWay}">
                    <StackPanel>
                        <!-- Header with Overall Rating and EXPERIMENTAL Badge -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Title with Experimental Badge -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="📊 Dither Quality Assessment"
                                          FontSize="14"
                                          FontWeight="Bold"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>

                                <!-- EXPERIMENTAL Badge -->
                                <Border Background="#D32F2F"
                                        CornerRadius="3"
                                        Padding="6,2"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="EXPERIMENTAL"
                                              FontSize="9"
                                              FontWeight="Bold"
                                              Foreground="White"
                                              VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>

                            <!-- Overall Rating Badge -->
                            <Border Grid.Column="1"
                                    CornerRadius="10"
                                    Padding="10,4"
                                    Margin="0,0,8,0">
                                <Border.Background>
                                    <SolidColorBrush Color="{Binding QualityRatingColor, Mode=OneWay}"/>
                                </Border.Background>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding QualityResult.QualityRating, Mode=OneWay}"
                                              FontWeight="Bold"
                                              FontSize="12"
                                              Foreground="White"
                                              Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding CombinedScoreValue, Mode=OneWay}"
                                              FontSize="12"
                                              Foreground="White"
                                              Opacity="0.9"/>
                                </StackPanel>
                            </Border>

                            <!-- Toggle Button for Quality Assessment -->
                            <CheckBox Grid.Column="2"
                                     IsChecked="{Binding IsQualityAssessmentEnabled, Mode=TwoWay}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,5,0"
                                     ToolTip="Enable/Disable Quality Assessment"
                                     Style="{DynamicResource ToggleSwitchStyle}"/>
                        </Grid>

                        <!-- Primary Metrics Grid -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Centered L2 Discrepancy -->
                            <Border Grid.Column="0"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8"
                                    Margin="0,0,4,0">
                                <StackPanel>
                                    <TextBlock Text="CD*"
                                              FontSize="9"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding CDValue, Mode=OneWay}"
                                              FontSize="14"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding CDRating, Mode=OneWay}"
                                              FontSize="10"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Drift Ratio (walking noise indicator) -->
                            <Border Grid.Column="1"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8"
                                    Margin="0,0,4,0"
                                    ToolTip="Drift ratio: net displacement / total path length. 0 = pattern returns to origin, 1 = one-directional drift (walking noise risk)">
                                <StackPanel>
                                    <TextBlock Text="Drift*"
                                              FontSize="9"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding DriftValue, Mode=OneWay}"
                                              FontSize="14"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DriftRating, Mode=OneWay}"
                                              FontSize="10"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- NNI -->
                            <Border Grid.Column="2"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8">
                                <StackPanel>
                                    <TextBlock Text="NNI*"
                                              FontSize="9"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding NNIValue, Mode=OneWay}"
                                              FontSize="14"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding NNIRating, Mode=OneWay}"
                                              FontSize="10"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Drizzle Gap-Fill Metrics -->
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="🔬 Drizzle Coverage Uniformity (min/mean weight per output pixel)"
                                      FontSize="10"
                                      FontWeight="SemiBold"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                      Margin="0,0,0,6"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 1x Drizzle -->
                                <Border Grid.Column="0"
                                        Background="{DynamicResource TertiaryBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="8"
                                        Margin="0,0,4,0">
                                    <StackPanel>
                                        <TextBlock Text="1× Drizzle"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding GFM1xValue, Mode=OneWay}"
                                                  FontSize="13"
                                                  FontWeight="Bold"
                                                  Foreground="{DynamicResource PrimaryBrush}"
                                                  HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding GFM1xTarget, Mode=OneWay}"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- 2x Drizzle -->
                                <Border Grid.Column="1"
                                        Background="{DynamicResource TertiaryBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="8"
                                        Margin="0,0,4,0">
                                    <StackPanel>
                                        <TextBlock Text="2× Drizzle"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding GFM2xValue, Mode=OneWay}"
                                                  FontSize="13"
                                                  FontWeight="Bold"
                                                  Foreground="{DynamicResource PrimaryBrush}"
                                                  HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding GFM2xTarget, Mode=OneWay}"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- 3x Drizzle -->
                                <Border Grid.Column="2"
                                        Background="{DynamicResource TertiaryBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="8">
                                    <StackPanel>
                                        <TextBlock Text="3× Drizzle"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding GFM3xValue, Mode=OneWay}"
                                                  FontSize="13"
                                                  FontWeight="Bold"
                                                  Foreground="{DynamicResource PrimaryBrush}"
                                                  HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding GFM3xTarget, Mode=OneWay}"
                                                  FontSize="9"
                                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>

                        <!-- Recommendation -->
                        <Border Background="{DynamicResource TertiaryBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Padding="10"
                                Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="💡 Recommendation"
                                          FontSize="10"
                                          FontWeight="SemiBold"
                                          Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                          Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding QualityResult.Recommendation, Mode=OneWay}"
                                          FontSize="10"
                                          TextWrapping="Wrap"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          LineHeight="14"/>
                            </StackPanel>
                        </Border>

                        <!-- Metric Settings (pixfrac, pixel scale conversion) -->
                        <StackPanel Orientation="Horizontal" Margin="2,0,0,8">
                            <TextBlock Text="pixfrac:"
                                      FontSize="9"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,4,0"/>
                            <TextBox Text="{Binding QualityPixfrac, StringFormat=F2}"
                                     Width="36"
                                     FontSize="9"
                                     VerticalAlignment="Center"
                                     Margin="0,0,10,0"
                                     ToolTip="Drizzle pixfrac used for the coverage simulation (0-1, default 0.6)"/>
                            <TextBlock Text="px ratio (0 = auto):"
                                      FontSize="9"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,4,0"/>
                            <TextBox Text="{Binding PixelScaleRatioOverride, StringFormat=F2}"
                                     Width="36"
                                     FontSize="9"
                                     VerticalAlignment="Center"
                                     Margin="0,0,10,0"
                                     ToolTip="Main-camera pixels per guide-camera pixel. 0 = derive automatically from PHD2 pixel scale and the NINA profile (camera pixel size / focal length)"/>
                            <TextBlock Text="{Binding EffectiveScaleRatioText, Mode=OneWay}"
                                      FontSize="9"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                      VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                      VerticalAlignment="Center"
                                      Margin="2,0,0,0">
                                <Run Text="Based on"
                                     Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                     FontSize="9"
                                     FontWeight="SemiBold"/>
                                <Run Text=" "/>
                                <Run Text="{Binding QualityResult.TotalDithers, StringFormat='{}{0} dithers', Mode=OneWay}"
                                     Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                     FontSize="9"
                                     FontWeight="SemiBold"/>
                            </TextBlock>

                            <StackPanel Grid.Column="1"
                                       Orientation="Horizontal">
                                <Button Content="🔄 Recalc"
                                        Command="{Binding RecalculateMetricsCommand}"
                                        Style="{StaticResource QualityButton}"
                                        Margin="0,0,4,0"
                                        ToolTip="Recalculate quality metrics"/>

                                <Button Content="💾 Export"
                                        Command="{Binding ExportMetricsCommand}"
                                        Style="{StaticResource QualityButton}"
                                        ToolTip="Export detailed report (TXT)"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Info Box when Quality Assessment is disabled or not enough dithers -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                       BorderBrush="{DynamicResource BorderBrush}"
                       BorderThickness="1"
                       Padding="20"
                       Margin="0,0,0,10">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <!-- Show when Quality Assessment is disabled OR when enabled but not enough data -->
                                <DataTrigger Binding="{Binding ShowQualityAssessment, Mode=OneWay}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Content (centered) -->
                        <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="📊"
                                      FontSize="20"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,8"/>
                            <TextBlock Text="Quality Assessment"
                                      FontSize="12"
                                      FontWeight="SemiBold"
                                      HorizontalAlignment="Center"
                                      Foreground="{DynamicResource PrimaryBrush}"
                                      Margin="0,0,0,4"/>
                            <!-- Show different text based on state -->
                            <TextBlock FontSize="10"
                                      HorizontalAlignment="Center"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Requires at least 4 dither events"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowQualityDisabledMessage, Mode=OneWay}" Value="True">
                                                <Setter Property="Text" Value="Quality Assessment disabled"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <!-- Toggle Button at top right -->
                        <CheckBox Grid.Row="0" Grid.Column="1"
                                 IsChecked="{Binding IsQualityAssessmentEnabled, Mode=TwoWay}"
                                 HorizontalAlignment="Right"
                                 VerticalAlignment="Top"
                                 Margin="0,0,5,0"
                                 ToolTip="Enable/Disable Quality Assessment"
                                 Style="{DynamicResource ToggleSwitchStyle}"/>
                    </Grid>
                </Border>

                <!-- Dither Settings Optimizer Panel - Shown when enabled and enough data -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                       BorderBrush="{DynamicResource BorderBrush}"
                       BorderThickness="1"
                       Padding="15"
                       Margin="0,0,0,10"
                       Visibility="{Binding ShowDitherOptimizer, Converter={StaticResource BoolToVis}, Mode=OneWay}">
                    <StackPanel>
                        <!-- Header with EXPERIMENTAL Badge and Toggle -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Dither Settings Optimizer"
                                          FontSize="14"
                                          FontWeight="Bold"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>
                                <Border Background="#D32F2F"
                                        CornerRadius="3"
                                        Padding="6,2"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="EXPERIMENTAL"
                                              FontSize="9"
                                              FontWeight="Bold"
                                              Foreground="White"
                                              VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>

                            <CheckBox Grid.Column="1"
                                     IsChecked="{Binding IsDitherOptimizerEnabled, Mode=TwoWay}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,5,0"
                                     ToolTip="Enable/Disable Dither Settings Optimizer"
                                     Style="{DynamicResource ToggleSwitchStyle}"/>
                        </Grid>

                        <!-- 3 Profile Columns: Strict (P90) / Standard (P95) / Fast (P99) -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Strict Profile (P90) -->
                            <Border Grid.Column="0"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8"
                                    Margin="0,0,4,0"
                                    ToolTip="Strict: waits until guiding is well inside its normal range. Highest confidence, longest settling — does NOT improve image quality.">
                                <StackPanel>
                                    <!-- Profile Header -->
                                    <TextBlock Text="Strict"
                                              FontSize="11"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="P90  /  slow"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,8"/>

                                    <!-- Settle Tolerance -->
                                    <TextBlock Text="Settle Pixel"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Distance from lock position PHD2 waits for after a dither (guide camera pixels)"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettlePixelQuality, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" px" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding SettleArcsecQuality, Mode=OneWay}"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,6"/>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Expected Settle Duration -->
                                    <TextBlock Text="Expected Settle"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Median time from dither until guiding stayed below this tolerance — for information, do NOT enter as min settle time"/>
                                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,6">
                                        <Run Text="{Binding ExpectedSettleQuality, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Settle Timeout -->
                                    <TextBlock Text="Settle Timeout"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Suggested settle timeout: covers 95% of observed settle delays plus margin"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettleTimeoutQuality, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <!-- Standard Profile (P95) -->
                            <Border Grid.Column="1"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8"
                                    Margin="0,0,4,0"
                                    ToolTip="Standard: good confidence that guiding is back to normal with moderate settle times.">
                                <StackPanel>
                                    <!-- Profile Header -->
                                    <TextBlock Text="Standard"
                                              FontSize="11"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="P95  /  balanced"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,8"/>

                                    <!-- Settle Tolerance -->
                                    <TextBlock Text="Settle Pixel"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Distance from lock position PHD2 waits for after a dither (guide camera pixels)"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettlePixelBalanced, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" px" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding SettleArcsecBalanced, Mode=OneWay}"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,6"/>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Expected Settle Duration -->
                                    <TextBlock Text="Expected Settle"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Median time from dither until guiding stayed below this tolerance — for information, do NOT enter as min settle time"/>
                                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,6">
                                        <Run Text="{Binding ExpectedSettleBalanced, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Settle Timeout -->
                                    <TextBlock Text="Settle Timeout"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Suggested settle timeout: covers 95% of observed settle delays plus margin"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettleTimeoutBalanced, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <!-- Fast Profile (P99) -->
                            <Border Grid.Column="2"
                                    Background="{DynamicResource TertiaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Padding="8"
                                    ToolTip="Fast: tolerance safely above the normal guiding scatter — settles quickly and rarely restarts. Recommended for most setups.">
                                <StackPanel>
                                    <!-- Profile Header -->
                                    <TextBlock Text="Fast"
                                              FontSize="11"
                                              FontWeight="Bold"
                                              Foreground="{DynamicResource PrimaryBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,2"/>
                                    <TextBlock Text="P99  /  recommended"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,8"/>

                                    <!-- Settle Tolerance -->
                                    <TextBlock Text="Settle Pixel"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Distance from lock position PHD2 waits for after a dither (guide camera pixels)"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettlePixelPerformance, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" px" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding SettleArcsecPerformance, Mode=OneWay}"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,6"/>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Expected Settle Duration -->
                                    <TextBlock Text="Expected Settle"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Median time from dither until guiding stayed below this tolerance — for information, do NOT enter as min settle time"/>
                                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,6">
                                        <Run Text="{Binding ExpectedSettlePerformance, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>

                                    <!-- Separator -->
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,0"
                                            Margin="4,0,4,6"/>

                                    <!-- Settle Timeout -->
                                    <TextBlock Text="Settle Timeout"
                                              FontSize="8"
                                              Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                              HorizontalAlignment="Center"
                                              ToolTip="Suggested settle timeout: covers 95% of observed settle delays plus margin"/>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="{Binding SettleTimeoutPerformance, Mode=OneWay}"
                                             FontSize="15" FontWeight="Bold"
                                             Foreground="{DynamicResource PrimaryBrush}"/>
                                        <Run Text=" s" FontSize="9"
                                             Foreground="{DynamicResource ButtonForegroundDisabledBrush}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Footer info -->
                        <TextBlock Text="{Binding RecommendationInfo, Mode=OneWay}"
                                  FontSize="9"
                                  Foreground="{DynamicResource ButtonForegroundDisabledBrush}"
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding RecommendationWarning, Mode=OneWay}"
                                  FontSize="9"
                                  Foreground="Orange"
                                  TextWrapping="Wrap"
                                  HorizontalAlignment="Center"
                                  Margin="0,4,0,0"
                                  Visibility="{Binding HasRecommendationWarning, Converter={StaticResource BoolToVis}, Mode=OneWay}"/>
                    </StackPanel>
                </Border>

                <!-- Info Box when Dither Optimizer is disabled or not enough data -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                       BorderBrush="{DynamicResource BorderBrush}"
                       BorderThickness="1"
                       Padding="20"
                       Margin="0,0,0,10">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowDitherOptimizer, Mode=OneWay}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Dither Settings Optimizer"
                                      FontSize="12"
                                      FontWeight="SemiBold"
                                      HorizontalAlignment="Center"
                                      Foreground="{DynamicResource PrimaryBrush}"
                                      Margin="0,0,0,4"/>
                            <TextBlock FontSize="10"
                                      HorizontalAlignment="Center"
                                      Foreground="{DynamicResource ButtonForegroundDisabledBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Requires at least 3 dither events"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowDitherOptimizerDisabledMessage, Mode=OneWay}" Value="True">
                                                <Setter Property="Text" Value="Dither Settings Optimizer disabled"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                                 IsChecked="{Binding IsDitherOptimizerEnabled, Mode=TwoWay}"
                                 HorizontalAlignment="Right"
                                 VerticalAlignment="Top"
                                 Margin="0,0,5,0"
                                 ToolTip="Enable/Disable Dither Settings Optimizer"
                                 Style="{DynamicResource ToggleSwitchStyle}"/>
                    </Grid>
                </Border>

                <!-- Action Buttons -->
                <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                       BorderBrush="{DynamicResource BorderBrush}"
                       BorderThickness="1"
                       Padding="10">
                    <StackPanel>
                        <TextBlock Text="Actions" 
                                  FontSize="14" 
                                  FontWeight="SemiBold" 
                                  Foreground="{DynamicResource PrimaryBrush}"
                                  Margin="0,0,0,10"/>

                        <UniformGrid Columns="1" Rows="2">
                            <Button Content="Clear Data" 
                                   Command="{Binding ClearDataCommand}" 
                                   Style="{StaticResource QualityButton}"
                                   Padding="10,8"
                                   Margin="0,0,0,5"/>

                            <Button Content="📄 Export Events CSV" 
                                   Command="{Binding ExportDitherEventsCsvCommand}" 
                                   Style="{StaticResource QualityButton}"
                                   Padding="10,8"
                                   ToolTip="Export dither events to CSV (timestamps, pixel shifts, settle times)"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
